# Loaded only by `npm run demo`. Overrides the real keys in .env so the preview cannot reach
# the production database; the app uses the in-memory demo client instead.
VITE_DEMO=1
VITE_SUPABASE_URL=http://127.0.0.1:9
VITE_SUPABASE_ANON_KEY=demo
